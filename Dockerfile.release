FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y --no-install-recommends ca-certificates && rm -rf /var/lib/apt/lists/*
COPY oih /usr/local/bin/oih
EXPOSE 9800
ENTRYPOINT ["oih"]
CMD ["-listen", "0.0.0.0:9800"]
